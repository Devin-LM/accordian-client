import Box from '@mui/material/Box';

function Friend() {
  return (
    <Box>
      {/* Should stricly contain routing content for routing to this 'friends' dms plus fallback handling */}
    </Box>
  )
}
export default Friend;
